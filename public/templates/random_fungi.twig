<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <link rel="stylesheet" href="/assets/css/random_fungi.css">
  <style>
    :root {
      --primary-color: #2c3e50;
      --secondary-color: #34495e;
      --accent-color: #3498db;
      --text-light: #ecf0f1;
      --text-dark: #2c3e50;
    }

    body {
      font-family: 'Montserrat', sans-serif;
      background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
      color: var(--text-light);
      min-height: 100vh;
      position: relative;
      padding-bottom: 60px;
    }

    .main-container {
      padding: 2rem 0;
    }

    .fungi-header {
      text-align: center;
      margin-bottom: 3rem;
      position: relative;
    }

    .fungi-header::after {
      content: '';
      display: block;
      width: 100px;
      height: 4px;
      background: var(--accent-color);
      margin: 1rem auto;
      border-radius: 2px;
    }

    .carousel-container {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      padding: 1rem;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    }

    .carousel-image {
      border-radius: 10px;
      height: 500px;
      object-fit: cover;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
    }

    .carousel-indicators li {
      background-color: var(--accent-color);
      opacity: 0.5;
    }

    .carousel-indicators .active {
      opacity: 1;
    }

    .info-card {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 15px;
      padding: 2rem;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      color: var(--text-dark);
      height: 100%;
      transition: transform 0.3s ease;
    }

    .info-card:hover {
      transform: translateY(-5px);
    }

    .section-title {
      color: var(--accent-color);
      font-weight: 700;
      margin-bottom: 1.5rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--accent-color);
    }

    .detail-section {
      background: #f8f9fa;
      border-radius: 10px;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .detail-item {
      margin-bottom: 0.8rem;
      display: flex;
      align-items: baseline;
    }

    .detail-item strong {
      min-width: 120px;
      color: var(--accent-color);
    }

    .edibility-badge {
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-weight: 500;
      display: inline-block;
      margin-top: 0.5rem;
    }

    .edibility-badge.comestible { background: #27ae60; color: white; }
    .edibility-badge.toxica { background: #e74c3c; color: white; }
    .edibility-badge.mortal { background: #2c3e50; color: white; }
    .edibility-badge.precaucion { background: #f1c40f; color: black; }

    .action-buttons {
      display: flex;
      gap: 1rem;
      margin-top: 2rem;
    }

    .btn-custom {
      padding: 0.8rem 1.5rem;
      border-radius: 25px;
      font-weight: 500;
      transition: all 0.3s ease;
      flex: 1;
    }

    .btn-custom:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
    }

    .btn-random {
      background: var(--accent-color);
      border: none;
      color: white;
    }

    .btn-share {
      background: #2ecc71;
      border: none;
      color: white;
    }

    footer {
      background: rgba(0, 0, 0, 0.2);
      padding: 1rem 0;
      position: absolute;
      bottom: 0;
      width: 100%;
      color: var(--text-light);
    }

    @media (max-width: 768px) {
      .carousel-image {
        height: 300px;
      }
      
      .info-card {
        margin-top: 2rem;
      }
      
      .detail-item {
        flex-direction: column;
      }
      
      .detail-item strong {
        margin-bottom: 0.3rem;
      }
    }
  </style>
</head>
<body>
  {% include 'header.twig' %}
  
  <div class="container main-container">
    {% if fungus %}
      <div class="fungi-header">
        <h1>{{ fungus.name }}</h1>
        {% if fungus.common_name %}
          <h3 class="text-muted">{{ fungus.common_name }}</h3>
        {% endif %}
      </div>

      <div class="row">
        <div class="col-lg-8">
          <div class="carousel-container">
            {% if fungus.image_urls %}
              <div id="fungusCarousel" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                  {% for image_url in fungus.image_urls|split(',') %}
                    <li data-target="#fungusCarousel" data-slide-to="{{ loop.index0 }}" {% if loop.first %}class="active"{% endif %}></li>
                  {% endfor %}
                </ol>
                <div class="carousel-inner">
                  {% for image_url in fungus.image_urls|split(',') %}
                    <div class="carousel-item {% if loop.first %}active{% endif %}">
                      <img src="{{ image_url }}" class="d-block w-100 carousel-image" alt="{{ fungus.name }}">
                    </div>
                  {% endfor %}
                </div>
                <a class="carousel-control-prev" href="#fungusCarousel" role="button" data-slide="prev">
                  <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                  <span class="sr-only">Anterior</span>
                </a>
                <a class="carousel-control-next" href="#fungusCarousel" role="button" data-slide="next">
                  <span class="carousel-control-next-icon" aria-hidden="true"></span>
                  <span class="sr-only">Siguiente</span>
                </a>
              </div>
            {% endif %}
          </div>
        </div>

        <div class="col-lg-4">
          <div class="info-card">
            <h4 class="section-title">Información General</h4>
            <div class="detail-item">
              <strong>Autor:</strong> {{ fungus.author }}
            </div>
            <div class="detail-item">
              <strong>Comestibilidad:</strong>
              <span class="edibility-badge {{ fungus.edibility }}">{{ fungus.edibility }}</span>
            </div>
            {% if fungus.synonym %}
              <div class="detail-item">
                <strong>Sinónimo:</strong> {{ fungus.synonym }}
              </div>
            {% endif %}

            <div class="detail-section">
              <h5 class="section-title">Taxonomía</h5>
              {% if fungus.division %}<div class="detail-item"><strong>División:</strong> {{ fungus.division }}</div>{% endif %}
              {% if fungus.subdivision %}<div class="detail-item"><strong>Subdivisión:</strong> {{ fungus.subdivision }}</div>{% endif %}
              {% if fungus.class %}<div class="detail-item"><strong>Clase:</strong> {{ fungus.class }}</div>{% endif %}
              {% if fungus.subclass %}<div class="detail-item"><strong>Subclase:</strong> {{ fungus.subclass }}</div>{% endif %}
              {% if fungus.ordo %}<div class="detail-item"><strong>Orden:</strong> {{ fungus.ordo }}</div>{% endif %}
              {% if fungus.family %}<div class="detail-item"><strong>Familia:</strong> {{ fungus.family }}</div>{% endif %}
            </div>

            <div class="action-buttons">
              <a href="/random" class="btn btn-custom btn-random">
                <i class="fas fa-random mr-2"></i>Otro Hongo
              </a>
              <button class="btn btn-custom btn-share">
                <i class="fas fa-share-alt mr-2"></i>Compartir
              </button>
              {% if session.isLoggedIn %}
                <form action="/fungus/like" method="POST" class="d-inline">
                  <input type="hidden" name="fungi_id" value="{{ fungus.id }}">
                  <button type="submit" class="btn btn-custom {% if fungus.is_liked %}btn-danger{% else %}btn-primary{% endif %}">
                    <i class="fas fa-heart mr-2"></i>
                    {% if fungus.is_liked %}
                      Quitar Me gusta
                    {% else %}
                      Me gusta
                    {% endif %}
                  </button>
                </form>
              {% endif %}
            </div>
          </div>
        </div>
      </div>

      <div class="row mt-4">
        <div class="col-md-6">
          <div class="info-card">
            <h4 class="section-title">Características</h4>
            {% if fungus.cap %}<div class="detail-item"><strong>Sombrero:</strong> {{ fungus.cap }}</div>{% endif %}
            {% if fungus.hymenium %}<div class="detail-item"><strong>Himenio:</strong> {{ fungus.hymenium }}</div>{% endif %}
            {% if fungus.stipe %}<div class="detail-item"><strong>Pie:</strong> {{ fungus.stipe }}</div>{% endif %}
            {% if fungus.flesh %}<div class="detail-item"><strong>Carne:</strong> {{ fungus.flesh }}</div>{% endif %}
          </div>
        </div>

        <div class="col-md-6">
          <div class="info-card">
            {% if fungus.habitat %}
              <h4 class="section-title">Hábitat</h4>
              <p>{{ fungus.habitat }}</p>
            {% endif %}
            
            {% if fungus.observations %}
              <h4 class="section-title mt-4">Observaciones</h4>
              <p>{{ fungus.observations }}</p>
            {% endif %}
          </div>
        </div>
      </div>
    {% else %}
      <div class="text-center info-card">
        <h2>No se encontró ningún hongo</h2>
        <a href="/random" class="btn btn-custom btn-random mt-3">Intentar de nuevo</a>
      </div>
    {% endif %}
  </div>

  <footer class="text-center">
    <div class="container">
      <p class="mb-0">&copy; {{ "now"|date("Y") }} Fungi Database. Todos los derechos reservados.</p>
    </div>
  </footer>

  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="/assets/js/script.js"></script>
</body>
</html>
