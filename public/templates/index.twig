<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <!-- Header con navbar -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="/">Fungi</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                </ul>
            </div>
        </nav>
    </header>

    <!-- Contenido principal -->
    <main class="container mt-5">
        <h1>{{ title }}</h1>
        <div class="fungi-grid">
            {% if fungi %}
                {% for fungus in fungi %}
                    <div class="fungi-card">
                        {% if fungus.image_urls %}
                            {% set first_image = fungus.image_urls|split(',')|first %}
                            <img src="{{ first_image }}" alt="{{ fungus.name }}" class="card-img-top">
                        {% endif %}
                        <div class="card-body">
                            <h5 class="card-title">{{ fungus.name }}</h5>
                            <p class="card-text">{{ fungus.common_name }}</p>
                        </div>
                    </div>
                {% endfor %}
            {% else %}
                <p>No se encontraron hongos.</p>
            {% endif %}
        </div>
    </main>

    <!-- Footer -->
    <footer class="text-center mt-5 p-3 bg-light">
        <p>&copy; 2025 Fungi. Todos los derechos reservados.</p>
    </footer>

    <!-- Incluir JS: jQuery, Popper.js, Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!-- Incluir JS personalizado -->
    <script src="/assets/js/script.js"></script>
</body>
</html>

