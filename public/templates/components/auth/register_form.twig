<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/assets/lib/twbs/bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/lib/animate.css/animate.min.css">
    <link rel="stylesheet" href="/assets/lib/aos/aos.css">
    <link rel="stylesheet" href="/assets/styles/pages/auth.css">
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card shadow-lg col-md-6 col-lg-5 col-xl-4 border-0 animate__animated animate__fadeIn" data-aos="zoom-in">
            <div class="card-header bg-primary text-white text-center py-3">
                <h2 class="mb-0 animate__animated animate__pulse animate__infinite animate__slower"><i class="fas fa-user-plus me-2"></i> {{ _('Registro') }}</h2>
            </div>
            <div class="card-body p-4">
                <div class="mb-3 text-start">
                    <a href="/" class="text-decoration-none text-secondary animate__animated animate__pulse animate__infinite">
                        <i class="fas fa-arrow-left me-1"></i> {{ _('Volver atrás') }}
                    </a>
                </div>
                
                {% if error %}
                <div class="alert alert-danger animate__animated animate__headShake">
                    <i class="fas fa-exclamation-circle me-2"></i> {{ error }}
                </div>
                {% endif %}

                {% if success %}
                <div class="alert alert-success animate__animated animate__bounceIn">
                    <i class="fas fa-check-circle me-2"></i> {{ success }}
                </div>
                {% endif %}

                <div id="registerError" class="alert alert-danger d-none animate__animated animate__headShake">
                    <i class="fas fa-exclamation-circle me-2"></i> <span id="errorMessage"></span>
                </div>

                <div id="registerSuccess" class="alert alert-success d-none animate__animated animate__bounceIn">
                    <i class="fas fa-check-circle me-2"></i> <span id="successMessage">{{ _('Registro exitoso. Serás redirigido al inicio de sesión...') }}</span>
                </div>

                <form id="registerForm" onsubmit="return handleRegister(event)" data-aos="fade-up" data-aos-delay="200">
                    <div class="mb-4">
                        <label for="username" class="form-label text-white"><i class="fas fa-user me-2"></i> {{ _('Usuario') }}</label>
                        <input type="text" class="form-control form-control-lg" id="username" name="username" required placeholder="{{ _('Elige un nombre de usuario') }}">
                    </div>

                    <div class="mb-4">
                        <label for="email" class="form-label text-white"><i class="fas fa-envelope me-2"></i> {{ _('Correo electrónico') }}</label>
                        <input type="email" class="form-control form-control-lg" id="email" name="email" required placeholder="{{ _('Introduce tu correo electrónico') }}">
                    </div>

                    <div class="mb-4">
                        <label for="password" class="form-label text-white"><i class="fas fa-lock me-2"></i> {{ _('Contraseña') }}</label>
                        <input type="password" class="form-control form-control-lg" id="password" name="password" required placeholder="{{ _('Elige una contraseña') }}">
                    </div>

                    <div class="mb-4">
                        <label for="confirm_password" class="form-label text-white"><i class="fas fa-lock me-2"></i> {{ _('Confirmar Contraseña') }}</label>
                        <input type="password" class="form-control form-control-lg" id="confirm_password" name="confirm_password" required placeholder="{{ _('Repite tu contraseña') }}">
                    </div>

                    <button type="submit" class="btn btn-primary w-100 py-2 animate__animated animate__pulse animate__infinite animate__slow">
                        <i class="fas fa-user-plus me-2"></i> {{ _('Registrarse') }}
                    </button>
                </form>
            </div>
            <div class="card-footer text-center py-3">
                <p class="mb-0 text-white"><i class="fas fa-sign-in-alt me-2"></i> {{ _('¿Ya tienes una cuenta?') }} <a href="/login" class="text-decoration-none text-info">{{ _('Inicia sesión') }}</a></p>
            </div>
        </div>
    </div>
    <script src="/assets/lib/aos/aos.js"></script>
    <script src="/assets/lib/twbs/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/scripts/register.js"></script>
</body>
</html>
