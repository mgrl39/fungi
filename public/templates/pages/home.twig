<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/assets/bootstrap/css/bootstrap.min.css">
    
    <!-- Estilos personalizados mínimos -->
    <link rel="stylesheet" href="/assets/styles/generic.css">
    <link rel="stylesheet" href="/assets/styles/components/navbar.css">
    <link rel="stylesheet" href="/assets/styles/components/footer.css">
    <link rel="stylesheet" href="/assets/styles/pages/home.css">
    
    <!-- Font Awesome para iconos (versión local) -->
    <link rel="stylesheet" href="/assets/fontawesome/css/all.min.css">
</head>
<body>
    <header>
        {% include 'components/navbar.twig' %}
    </header>

    <main>
        <!-- Sección Hero -->
        <section class="hero">
            <div class="container">
                <h1 class="hero-title">{{ title }}</h1>
                <p class="hero-text">Descubre la diversidad de hongos, aprende sobre sus características y explora su taxonomía en nuestra base de datos completa.</p>
                <div class="hero-actions">
                    <a href="/random" class="btn btn-primary btn-lg"><i class="fas fa-random"></i> Hongo Aleatorio</a>
                    <a href="/statistics" class="btn btn-outline-light btn-lg"><i class="fas fa-chart-bar"></i> Ver Estadísticas</a>
                </div>
            </div>
        </section>

        <div class="container">
            {% if error %}
                <div class="alert alert-danger">
                    {{ error }}
                </div>
            {% endif %}

            <!-- Sección de Búsqueda -->
            <section class="search-section">
                <h2 class="section-title"><i class="fas fa-search"></i> Buscar Hongos</h2>
                <form class="search-form" action="/search" method="GET">
                    <div class="search-group">
                        <label for="search-name" class="search-label">Nombre</label>
                        <input type="text" id="search-name" name="name" class="form-control search-input" placeholder="Buscar por nombre...">
                    </div>
                    <div class="search-group">
                        <label for="search-edibility" class="search-label">Comestibilidad</label>
                        <select id="search-edibility" name="edibility" class="form-control search-input">
                            <option value="">Todos</option>
                            <option value="comestible">Comestible</option>
                            <option value="no-comestible">No Comestible</option>
                            <option value="venenoso">Venenoso</option>
                        </select>
                    </div>
                    <div class="search-group">
                        <button type="submit" class="btn btn-primary btn-block search-button">
                            <i class="fas fa-search"></i> Buscar
                        </button>
                    </div>
                </form>
            </section>

            <!-- Sección de Hongos Destacados -->
            <section class="featured-section">
                <h2 class="section-title"><i class="fas fa-star"></i> Hongos Destacados</h2>
                <p class="section-subtitle">Explora nuestra selección de hongos más interesantes</p>
                <div id="fungi-container" class="fungi-grid">
                    <p class="text-center w-100">Cargando hongos...</p>
                </div>
            </section>

            <!-- Sección de Categorías -->
            <section class="categories-section">
                <h2 class="section-title"><i class="fas fa-tags"></i> Explora por Categorías</h2>
                <div class="fungi-grid">
                    <a href="/category/comestible" class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-utensils"></i>
                        </div>
                        <h3 class="category-title">Comestibles</h3>
                        <span class="category-count">Explora hongos comestibles</span>
                    </a>
                    <a href="/category/medicinal" class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-mortar-pestle"></i>
                        </div>
                        <h3 class="category-title">Medicinales</h3>
                        <span class="category-count">Propiedades curativas</span>
                    </a>
                    <a href="/category/toxic" class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-skull-crossbones"></i>
                        </div>
                        <h3 class="category-title">Tóxicos</h3>
                        <span class="category-count">Hongos peligrosos</span>
                    </a>
                    <a href="/category/rare" class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-gem"></i>
                        </div>
                        <h3 class="category-title">Raros</h3>
                        <span class="category-count">Especies poco comunes</span>
                    </a>
                </div>
            </section>

            <!-- Sección CTA -->
            <section class="cta-section">
                <h2 class="cta-title">¿Conoces un hongo que no está en nuestra base de datos?</h2>
                <p class="cta-text">Ayúdanos a expandir nuestra colección de información micológica. Regístrate y contribuye con tus conocimientos.</p>
                <a href="/register" class="btn btn-primary btn-lg">Únete a nuestra comunidad</a>
            </section>
        </div>
    </main>

    <footer>
        {% include 'components/footer.twig' %}
    </footer>

    <!-- Scripts -->
    <script src="/assets/js/jquery.min.js"></script>
    <script src="/assets/bootstrap/js/bootstrap.bundle.min.js"></script>
    
    {% if use_ajax %}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Obtener hongos desde la API
            fetch('{{ api_url }}')
                .then(response => response.json())
                .then(data => {
                    if (data.success && data.data.length > 0) {
                        const fungiContainer = document.getElementById('fungi-container');
                        fungiContainer.innerHTML = ''; // Limpiar mensaje de carga
                        
                        // Generar HTML para cada hongo
                        data.data.forEach(fungi => {
                            // Obtener la primera imagen (si existe)
                            const imageUrls = fungi.image_urls ? fungi.image_urls.split(',') : [];
                            const imageUrl = imageUrls.length > 0 ? imageUrls[0] : '/assets/images/placeholder.jpg';
                            
                            // Generar el HTML para la tarjeta
                            const fungiCard = document.createElement('div');
                            fungiCard.className = 'card h-100';
                            fungiCard.innerHTML = `
                                <img src="${imageUrl}" class="card-img-top" alt="${fungi.name}">
                                <div class="card-body">
                                    <h5 class="card-title">${fungi.name.replace(/-/g, ' ')}</h5>
                                    <p class="card-text">${fungi.edibility.replace(/-/g, ' ')}</p>
                                    <a href="/fungi/${fungi.id}" class="btn btn-primary">Ver detalles</a>
                                </div>
                            `;
                            
                            // Crear el contenedor y añadir la tarjeta
                            const cardContainer = document.createElement('div');
                            cardContainer.appendChild(fungiCard);
                            fungiContainer.appendChild(cardContainer);
                        });
                    } else {
                        document.getElementById('fungi-container').innerHTML = 
                            '<p class="text-center w-100">{{ _("No hay hongos disponibles en este momento.") }}</p>';
                    }
                })
                .catch(error => {
                    console.error('Error al cargar los hongos:', error);
                    document.getElementById('fungi-container').innerHTML = 
                        '<p class="text-center w-100 text-danger">Error al cargar los hongos. Intente nuevamente.</p>';
                });
        });
    </script>
    {% endif %}
</body>
</html>
