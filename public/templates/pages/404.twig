<!DOCTYPE html>
<html lang="{{ idioma_actual }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ _('Página no encontrada') }}</title>
    <link rel="stylesheet" href="/assets/lib/twbs/bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/lib/fontawesome/fontawesome-free-6.4.0-web/css/all.min.css">
</head>
<body style="background-color: #131828;">
    
    <div class="container min-vh-100 d-flex align-items-center justify-content-center py-5">
        <div class="card animate__animated animate__fadeInUp border-0 rounded-4 text-center shadow-lg" style="max-width: 550px">
            <div class="card-header bg-primary bg-gradient text-white p-4 border-0">
                <i class="fas fa-exclamation-triangle display-1 animate__animated animate__tada animate__delay-1s mb-3"></i>
                <h1 class="display-4 fw-bold animate__animated animate__bounceIn animate__delay-1s">404</h1>
            </div>
            
            <div class="card-body p-4 p-lg-5">
                <h2 class="fs-2 fw-semibold mb-4 text-primary">
                    {{ _('Página no encontrada') }}
                </h2>
                
                <p class="text-secondary fs-5 mb-4">
                    {{ _('Lo sentimos, la página que buscas no existe o ha sido movida a otra ubicación.') }}
                </p>
                
                <div class="d-flex justify-content-center gap-3 flex-wrap mb-4">
                    <a href="/" class="btn btn-primary btn-lg fw-semibold animate__animated animate__pulse animate__infinite animate__slower">
                        <i class="fas fa-home me-2"></i> {{ _('Volver al inicio') }}
                    </a>
                    <a href="/contact" class="btn btn-outline-primary btn-lg fw-semibold">
                        <i class="fas fa-envelope me-2"></i> {{ _('Contactar') }}
                    </a>
                </div>
                
                <div class="alert alert-info my-4" role="alert">
                    <i class="fas fa-info-circle me-2"></i> 
                    {{ _('Si crees que esto es un error, por favor contacta con nuestro equipo de soporte.') }}
                </div>
            </div>
            
            <div class="card-footer py-3 bg-light border-0">
                <a href="javascript:history.back()" class="btn btn-link text-decoration-none">
                    <i class="fas fa-arrow-left me-1"></i> {{ _('Volver atrás') }}
                </a>
            </div>
        </div>
    </div>

    <!-- Scripts de Bootstrap -->
    <script src="/assets/lib/jquery.min.js"></script>
    <script src="/assets/lib/twbs/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
