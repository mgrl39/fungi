<!DOCTYPE html>
<html lang="{{ idioma_actual }}" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ api_docs.name }} - {{ api_docs.version }}</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Estilos personalizados mínimos -->
    <link rel="stylesheet" href="/assets/styles/generic.css">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Estilos inline -->
    <style>
        .hero {
            background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), 
                        url('/assets/images/api-bg.jpg') no-repeat center center;
            background-size: cover;
            padding: 5rem 0;
            margin-bottom: 2rem;
        }
        
        .endpoint-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .endpoint-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }
        
        .method-get { 
            background-color: #2563eb33;
            color: #60a5fa;
            border: 1px solid #2563eb4d;
        }
        
        .method-post {
            background-color: #16a34a33;
            color: #4ade80;
            border: 1px solid #16a34a4d;
        }
        
        .method-put {
            background-color: #d9770633;
            color: #fbbf24;
            border: 1px solid #d9770633;
        }
        
        .method-delete {
            background-color: #dc262633;
            color: #f87171;
            border: 1px solid #dc26264d;
        }
        
        .http-method {
            font-size: 0.75rem;
            padding: 0.125rem 0.5rem;
            border-radius: 0.25rem;
            font-weight: 600;
            text-transform: uppercase;
            display: inline-block;
            margin-right: 0.5rem;
        }
        
        .code-block {
            background-color: #1a1a1a;
            color: #f8f8f2;
            padding: 1rem;
            border-radius: 0.375rem;
            font-family: monospace;
            overflow-x: auto;
            white-space: pre;
        }
    </style>
</head>
<body>
    <header>
        {% include components.navbar %}
    </header>

    <main>
        <!-- Sección Hero -->
        <section class="hero text-center text-white">
            <div class="container">
                <h1 class="display-4 fw-bold mb-4">{{ api_docs.name }}</h1>
                <p class="lead mb-5">API RESTful</p>
                <div class="d-flex gap-3 justify-content-center">
                    <a href="https://github.com/tu-usuario/fungi-api" target="_blank" class="btn btn-outline-light btn-lg">
                        <i class="fab fa-github me-2"></i> GitHub
                    </a>
                    <a href="#endpoints" class="btn btn-primary btn-lg">
                        <i class="fas fa-book-open me-2"></i> API
                    </a>
                </div>
            </div>
        </section>

        <div class="container py-4">
            <div class="row mb-5">
                <div class="col-md-8">
                    <h2>API v{{ api_docs.version }}</h2>
                    <p>FungiAPI</p>
                </div>
                <div class="col-md-4">
                    <div class="card bg-dark border-0 shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title"><i class="fas fa-info-circle me-2"></i> Base URL</h5>
                            <div class="code-block">{{ app.request.schemeAndHttpHost }}/api</div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Tabs de métodos HTTP -->
            <div id="endpoints" class="mb-5">
                <h2 class="mb-4"><i class="fas fa-plug me-2"></i> Endpoints</h2>
                
                <ul class="nav nav-pills mb-4" id="httpMethodsTabs" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="all-tab" data-bs-toggle="pill" data-bs-target="#all" type="button" role="tab">
                            <i class="fas fa-globe me-1"></i> ALL
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="get-tab" data-bs-toggle="pill" data-bs-target="#get" type="button" role="tab">
                            <i class="fas fa-download me-1"></i> GET
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="post-tab" data-bs-toggle="pill" data-bs-target="#post" type="button" role="tab">
                            <i class="fas fa-plus me-1"></i> POST
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="put-tab" data-bs-toggle="pill" data-bs-target="#put" type="button" role="tab">
                            <i class="fas fa-pen me-1"></i> PUT
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="delete-tab" data-bs-toggle="pill" data-bs-target="#delete" type="button" role="tab">
                            <i class="fas fa-trash me-1"></i> DELETE
                        </button>
                    </li>
                </ul>
                
                <div class="tab-content" id="httpMethodsContent">
                    <!-- Todos los endpoints -->
                    <div class="tab-pane fade show active" id="all" role="tabpanel" aria-labelledby="all-tab">
                        <div class="row">
                            {% for endpoint in api_docs.endpoints %}
                                <div class="col-md-6 mb-4">
                                    <div class="card endpoint-card h-100">
                                        <div class="card-header">
                                            <span class="http-method method-{{ endpoint.method|lower }}">{{ endpoint.method }}</span>
                                            <span class="fw-bold">{{ endpoint.path }}</span>
                                        </div>
                                        <div class="card-body">
                                            {% if endpoint.requires_auth %}
                                                <div class="mb-2">
                                                    <span class="badge bg-info"><i class="fas fa-lock me-1"></i></span>
                                                </div>
                                            {% endif %}
                                            
                                            {% if endpoint.requires_admin %}
                                                <div class="mb-2">
                                                    <span class="badge bg-warning text-dark"><i class="fas fa-user-shield me-1"></i></span>
                                                </div>
                                            {% endif %}
                                            
                                            {% if endpoint.parameters is defined and endpoint.parameters|length > 0 %}
                                                <h6 class="mt-3">Params:</h6>
                                                <ul class="list-group list-group-flush">
                                                    {% for param, desc in endpoint.parameters %}
                                                        <li class="list-group-item bg-transparent border-0 px-0 py-1">
                                                            <code>{{ param }}</code>
                                                        </li>
                                                    {% endfor %}
                                                </ul>
                                            {% endif %}
                                        </div>
                                        <div class="card-footer bg-dark">
                                            <button class="btn btn-sm btn-outline-light" type="button" data-bs-toggle="collapse" data-bs-target="#example-{{ loop.index }}">
                                                <i class="fas fa-code me-1"></i> <i class="fas fa-terminal"></i>
                                            </button>
                                            
                                            <div class="collapse mt-2" id="example-{{ loop.index }}">
                                                <div class="code-block">
# cURL
curl -X {{ endpoint.method }} \
  "{{ app.request.schemeAndHttpHost }}{{ endpoint.path }}" \
{% if endpoint.requires_auth %}
  -H "Authorization: Bearer YOUR_TOKEN" \
{% endif %}
  -H "Content-Type: application/json"
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                    
                    <!-- GET endpoints -->
                    <div class="tab-pane fade" id="get" role="tabpanel" aria-labelledby="get-tab">
                        <div class="row">
                            {% for endpoint in api_docs.endpoints %}
                                {% if endpoint.method == 'GET' %}
                                    <div class="col-md-6 mb-4">
                                        <div class="card endpoint-card h-100">
                                            <div class="card-header">
                                                <span class="http-method method-get">GET</span>
                                                <span class="fw-bold">{{ endpoint.path }}</span>
                                            </div>
                                            <div class="card-body">
                                                {% if endpoint.requires_auth %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-info"><i class="fas fa-lock me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.requires_admin %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-warning text-dark"><i class="fas fa-user-shield me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.parameters is defined and endpoint.parameters|length > 0 %}
                                                    <h6 class="mt-3">Params:</h6>
                                                    <ul class="list-group list-group-flush">
                                                        {% for param, desc in endpoint.parameters %}
                                                            <li class="list-group-item bg-transparent border-0 px-0 py-1">
                                                                <code>{{ param }}</code>
                                                            </li>
                                                        {% endfor %}
                                                    </ul>
                                                {% endif %}
                                            </div>
                                            <div class="card-footer bg-dark">
                                                <button class="btn btn-sm btn-outline-light" type="button" data-bs-toggle="collapse" data-bs-target="#example-get-{{ loop.index }}">
                                                    <i class="fas fa-code me-1"></i> <i class="fas fa-terminal"></i>
                                                </button>
                                                
                                                <div class="collapse mt-2" id="example-get-{{ loop.index }}">
                                                    <div class="code-block">
# cURL
curl -X GET \
  "{{ app.request.schemeAndHttpHost }}{{ endpoint.path }}" \
{% if endpoint.requires_auth %}
  -H "Authorization: Bearer YOUR_TOKEN" \
{% endif %}
  -H "Content-Type: application/json"
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                    
                    <!-- POST endpoints -->
                    <div class="tab-pane fade" id="post" role="tabpanel" aria-labelledby="post-tab">
                        <div class="row">
                            {% for endpoint in api_docs.endpoints %}
                                {% if endpoint.method == 'POST' %}
                                    <div class="col-md-6 mb-4">
                                        <div class="card endpoint-card h-100">
                                            <div class="card-header">
                                                <span class="http-method method-post">POST</span>
                                                <span class="fw-bold">{{ endpoint.path }}</span>
                                            </div>
                                            <div class="card-body">
                                                {% if endpoint.requires_auth %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-info"><i class="fas fa-lock me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.requires_admin %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-warning text-dark"><i class="fas fa-user-shield me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.parameters is defined and endpoint.parameters|length > 0 %}
                                                    <h6 class="mt-3">Params:</h6>
                                                    <ul class="list-group list-group-flush">
                                                        {% for param, desc in endpoint.parameters %}
                                                            <li class="list-group-item bg-transparent border-0 px-0 py-1">
                                                                <code>{{ param }}</code>
                                                            </li>
                                                        {% endfor %}
                                                    </ul>
                                                {% endif %}
                                            </div>
                                            <div class="card-footer bg-dark">
                                                <button class="btn btn-sm btn-outline-light" type="button" data-bs-toggle="collapse" data-bs-target="#example-post-{{ loop.index }}">
                                                    <i class="fas fa-code me-1"></i> <i class="fas fa-terminal"></i>
                                                </button>
                                                
                                                <div class="collapse mt-2" id="example-post-{{ loop.index }}">
                                                    <div class="code-block">
# cURL
curl -X POST \
  "{{ app.request.schemeAndHttpHost }}{{ endpoint.path }}" \
{% if endpoint.requires_auth %}
  -H "Authorization: Bearer YOUR_TOKEN" \
{% endif %}
  -H "Content-Type: application/json" \
  -d '{
  {% for param, desc in endpoint.parameters %}
    "{{ param }}": "value"{% if not loop.last %},{% endif %}
  {% else %}
    "example": "data"
  {% endfor %}
  }'
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                    
                    <!-- PUT endpoints -->
                    <div class="tab-pane fade" id="put" role="tabpanel" aria-labelledby="put-tab">
                        <div class="row">
                            {% for endpoint in api_docs.endpoints %}
                                {% if endpoint.method == 'PUT' %}
                                    <div class="col-md-6 mb-4">
                                        <div class="card endpoint-card h-100">
                                            <div class="card-header">
                                                <span class="http-method method-put">PUT</span>
                                                <span class="fw-bold">{{ endpoint.path }}</span>
                                            </div>
                                            <div class="card-body">
                                                {% if endpoint.requires_auth %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-info"><i class="fas fa-lock me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.requires_admin %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-warning text-dark"><i class="fas fa-user-shield me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.parameters is defined and endpoint.parameters|length > 0 %}
                                                    <h6 class="mt-3">Params:</h6>
                                                    <ul class="list-group list-group-flush">
                                                        {% for param, desc in endpoint.parameters %}
                                                            <li class="list-group-item bg-transparent border-0 px-0 py-1">
                                                                <code>{{ param }}</code>
                                                            </li>
                                                        {% endfor %}
                                                    </ul>
                                                {% endif %}
                                            </div>
                                            <div class="card-footer bg-dark">
                                                <button class="btn btn-sm btn-outline-light" type="button" data-bs-toggle="collapse" data-bs-target="#example-put-{{ loop.index }}">
                                                    <i class="fas fa-code me-1"></i> <i class="fas fa-terminal"></i>
                                                </button>
                                                
                                                <div class="collapse mt-2" id="example-put-{{ loop.index }}">
                                                    <div class="code-block">
# cURL
curl -X PUT \
  "{{ app.request.schemeAndHttpHost }}{{ endpoint.path }}" \
{% if endpoint.requires_auth %}
  -H "Authorization: Bearer YOUR_TOKEN" \
{% endif %}
  -H "Content-Type: application/json" \
  -d '{
  {% for param, desc in endpoint.parameters %}
    "{{ param }}": "value"{% if not loop.last %},{% endif %}
  {% else %}
    "example": "data"
  {% endfor %}
  }'
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                    
                    <!-- DELETE endpoints -->
                    <div class="tab-pane fade" id="delete" role="tabpanel" aria-labelledby="delete-tab">
                        <div class="row">
                            {% for endpoint in api_docs.endpoints %}
                                {% if endpoint.method == 'DELETE' %}
                                    <div class="col-md-6 mb-4">
                                        <div class="card endpoint-card h-100">
                                            <div class="card-header">
                                                <span class="http-method method-delete">DELETE</span>
                                                <span class="fw-bold">{{ endpoint.path }}</span>
                                            </div>
                                            <div class="card-body">
                                                {% if endpoint.requires_auth %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-info"><i class="fas fa-lock me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.requires_admin %}
                                                    <div class="mb-2">
                                                        <span class="badge bg-warning text-dark"><i class="fas fa-user-shield me-1"></i></span>
                                                    </div>
                                                {% endif %}
                                                
                                                {% if endpoint.parameters is defined and endpoint.parameters|length > 0 %}
                                                    <h6 class="mt-3">Params:</h6>
                                                    <ul class="list-group list-group-flush">
                                                        {% for param, desc in endpoint.parameters %}
                                                            <li class="list-group-item bg-transparent border-0 px-0 py-1">
                                                                <code>{{ param }}</code>
                                                            </li>
                                                        {% endfor %}
                                                    </ul>
                                                {% endif %}
                                            </div>
                                            <div class="card-footer bg-dark">
                                                <button class="btn btn-sm btn-outline-light" type="button" data-bs-toggle="collapse" data-bs-target="#example-delete-{{ loop.index }}">
                                                    <i class="fas fa-code me-1"></i> <i class="fas fa-terminal"></i>
                                                </button>
                                                
                                                <div class="collapse mt-2" id="example-delete-{{ loop.index }}">
                                                    <div class="code-block">
# cURL
curl -X DELETE \
  "{{ app.request.schemeAndHttpHost }}{{ endpoint.path }}" \
{% if endpoint.requires_auth %}
  -H "Authorization: Bearer YOUR_TOKEN" \
{% endif %}
  -H "Content-Type: application/json"
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="mt-auto">
        {% include components.footer %}
    </footer>

    <!-- Bootstrap 5 JS Bundle con Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
